{
  "id": "wendl",
  "name": "Wendl",
  "description": "Budget-aware routing companion for a LiteLLM proxy: /limits shows per-key caps + spend, with zero token cost.",
  "version": "0.1.0",
  "commandAliases": [
    { "name": "limits", "kind": "runtime-slash", "cliCommand": "limits" },
    { "name": "stats", "kind": "runtime-slash", "cliCommand": "stats" },
    { "name": "wendl", "kind": "runtime-slash", "cliCommand": "wendl" }
  ],
  "activation": {
    "onStartup": false,
    "onCommands": ["limits", "stats", "wendl"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "litellmBaseUrl": { "type": "string", "default": "http://localhost:4000" },
      "litellmMasterKey": { "type": "string" },
      "globalCapUsd": { "type": "number" },
      "keys": {
        "type": "array",
        "items": { "type": "string" },
        "default": ["team", "ops"]
      }
    },
    "required": ["litellmMasterKey"]
  },
  "uiHints": {
    "litellmMasterKey": { "label": "LiteLLM master key", "placeholder": "sk-...", "sensitive": true },
    "litellmBaseUrl": { "label": "LiteLLM base URL", "placeholder": "http://localhost:4000" },
    "globalCapUsd": { "label": "Global safety cap (USD / 30d)", "placeholder": "300" }
  }
}
